# Assignment-2

صاحب العمل : سعود عبدالله المطيري 

1. نظرة عامة على المشروع (Project Overview)
يهدف هذا المشروع إلى بناء خط إنتاج متكامل لاكتشاف الكائنات في الصور (Object Detection Pipeline) باستخدام أحدث نماذج الذكاء الاصطناعي YOLO26. تضمن العمل تنفيذ الخطوات التالية برمجياً:

تحميل وتجهيز بيئة العمل على Google Colab.

جلب صورة مخصصة للاعب كرة سلة.

تشغيل نموذج الذكاء الاصطناعي للتنبؤ بمحتويات الصورة مع تحديد نسبة الثقة (Confidence Threshold).

الرسم اليدوي (Manual Plotting): تجاوز دالة العرض الجاهزة في الموديل ورسم الصناديق الحدودية (Bounding Boxes) والكتابة عليها برمجياً باستخدام مكتبة PIL.

توليد تقرير وصف نصي تلقائي (Descriptive Summary) يعبر عن محتوى المشهد بناءً على النتائج المكتشفة.


2. الأدوات والتقنيات المستخدمة (Tool Environment)
تم الاعتماد على التقنيات والمكتبات التالية:

Google Colab: بيئة التطوير السحابية لتشغيل وتنفيذ الأكواد.

Python: لغة البرمجة الأساسية لكتابة السكريبتات.

Ultralytics YOLO26: مكتبة النمذجة الحديثة لتنفيذ عملية الـ Inference واستخراج الإحداثيات والتصنيفات.

OpenCV & NumPy: لمعالجة المصفوفات والبيانات الخاصة بالصور والإحداثيات.

PIL (Pillow) & Matplotlib: للتعامل مع الصور، وتطبيق الرسم اليدوي للمستطيلات (Boxes)، وكتابة النصوص (Labels)، وعرض النتيجة النهائية.

Requests & IO: لجلب الصورة المطلوبة من الإنترنت برمجياً.




3. خطوات التنفيذ بالتفصيل (Step-by-Step Implementation)
الخطوة الأولى: إعداد البيئة وتثبيت المكتبات
تم تثبيت أحدث إصدار من مكتبة Ultralytics السحابية.

استيراد الحزم الضرورية مثل YOLO, cv2, numpy, matplotlib, و PIL.



الخطوة الثانية: تحميل الصورة وتشغيل الموديل
تم جلب صورة عالية الجودة للاعب كرة سلة يحمل الكرة من خلال رابط مباشر وحفظها محلياً في بيئة العمل باسم basketball_player.jpg.

تحميل موديل YOLO26 (النسخة الخفيفة yolo26n.pt لضمان سرعة التنفيذ).

تطبيق الحد الأدنى للثقة (Confidence Threshold = 0.45) لتصفية النتائج الضعيفة وضمان دقة الاكتشاف.



الخطوة الثالثة: الرسم اليدوي للصناديق الحدودية (Manual Bounding Boxes)
بدلاً من الاعتماد على دالة العرض التلقائية result.plot()، قمنا بـ:

استخراج الإحداثيات الأربعة لكل كائن مكتشف (xyxy), بالإضافة إلى نسبة الثقة (confidence) ومعرّف الفئة (class_id).

تفعيل مكتبة PIL (ImageDraw) لنسخ الصورة الأصلية والرسم عليها يدوياً.

رسم مستطيل بلون مميز (lime) حول الكائن المستهدف بناءً على الإحداثيات (x1, y1, x2, y2).

استخراج اسم الفئة المقابلة من قاموس الموديل (names) وكتابة اسم الكائن مع نسبة الثقة مباشرة فوق الحد العلوي للصندوق.



الخطوة الرابعة: توليد التقرير الوصف النصي التلقائي (Descriptive Summary Script)
بناء دالة برمجية (generate_scene_summary) تقوم بما يلي:

حساب عدد وحجم الكائنات المكتشفة وتصنيفها.

صياغة جملة إنجليزية طبيعية تصف محتويات الصورة (مثلاً: ذكر الكائنات المفردة أو الجمع بشكل تلقائي).

طباعة تقرير مفصل يوضح أن خط إنتاج الذكاء الاصطناعي قام بتحليل الإطار بنجاح وتوثيق كل عنصر تم رصده.
